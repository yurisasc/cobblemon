{{ license }}

package com.cobblemon.mod.common

object CobblemonConstants {

    const val MOD_ID = "{{ modid }}"
    const val VERSION_FULL = "{{ version }}"
    const val MOD_VERSION = "{{ modVersion }}"
    const val GAME_VERSION = "{{ gameVersion }}"
    const val SNAPSHOT = {{ isSnapshot }}

    const val GIT_COMMIT = "{{ gitCommit | default("unknown") }}"
    const val BUILD_NUMBER = "{{ buildNumber | default("Not Specified") }}"
    const val TIMESTAMP = "{{ timestamp }}"

}